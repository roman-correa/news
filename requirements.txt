# Pinned to exact local versions — must match the environment where artifacts were pickled.
# Unpickling sklearn preprocessors and XGBoost/LightGBM/CatBoost models requires
# identical library versions on both the training and serving machines.

scikit-learn==1.6.1
xgboost==3.0.3
lightgbm==4.6.0
catboost==1.2.8
numpy==2.3.2
pandas==2.3.1
geopandas==1.1.1
streamlit==1.44.1
plotly==6.2.0
optuna==4.7.0
mlflow==3.10.1
geopy==2.4.1
requests==2.32.3
rapidfuzz==3.14.1

# Geo dependencies (required by geopandas)
shapely>=2.0
pyproj>=3.6
fiona>=1.9